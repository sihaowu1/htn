{
  "version": 1,
  "startUrl": "https://replace-with-your-tunnel.example/",
  "rootId": "home",
  "status": "provided",
  "notes": [
    "Example for tests/fixtures/site.html. Replace startUrl with your target tunnel URL.",
    "For a manual map, omit fingerprint and provide each state's expected visible text and URL path. Update selectors and transitions for your actual site.",
    "For a discovered map, retain fingerprints to enforce exact normalized DOM matching."
  ],
  "states": [
    { "id": "home", "depth": 0, "snapshot": { "url": "https://replace-with-your-tunnel.example/", "text": "Test shop" } },
    { "id": "search-results", "depth": 1, "snapshot": { "url": "https://replace-with-your-tunnel.example/?q=apple", "text": "Found apple" } },
    { "id": "checkout", "depth": 2, "snapshot": { "url": "https://replace-with-your-tunnel.example/?view=checkout", "text": "Checkout" } }
  ],
  "transitions": [
    {
      "id": "search-apple", "from": "home", "to": "search-results", "status": "observed", "reason": "Manually supplied flow",
      "actions": [
        { "kind": "fill", "selector": "#query", "value": "apple" },
        { "kind": "click", "selector": "#search button", "value": "" }
      ]
    },
    {
      "id": "go-checkout", "from": "search-results", "to": "checkout", "status": "observed", "reason": "Skip this transition for search-only tasks",
      "actions": [ { "kind": "click", "selector": "a[href='/?view=checkout']", "value": "" } ]
    }
  ]
}
